import Roadmap from './roadmap'

export default {
  title: 'Project/Pages/About/Roadmap',
};

const Template = args => <Roadmap {...args} />;

export const Controls = Template.bind({});

Controls.args = {
};
